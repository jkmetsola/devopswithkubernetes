---
name: Delete branch
description: Delete deployments related to branch
inputs:
  project:
    description: "The project to delete"
    required: true
runs:
  using: "composite"
  steps:
    - shell: bash
      run: tools/gh-workflows/delete_branch_namespace.sh "${{ inputs.project }}"
